'use client';import {useEffect,useState} from 'react';import {api} from '../../../lib/api';import {getQueuedReports,clearQueued} from '../../../lib/offline';export default function Page(){const[q,setQ]=useState<any[]>([]);const load=()=>getQueuedReports().then(setQ);useEffect(()=>{load()},[]);const sync=async()=>{if(!navigator.onLine){alert('Still offline');return}const x=await api('/api/v1/sync',{method:'POST',body:JSON.stringify(q)});const done=x.results.filter((r:any)=>r.status==='accepted'||r.status==='duplicate').map((r:any)=>r.idempotency_key);await clearQueued(done);load()};return <main className="safety"><h1 className="title">Sync Queue</h1><div className="card"><h2>{q.length} reports queued</h2><button className="btn btn-primary" onClick={sync}>Sync now</button>{q.map(x=><p key={x.idempotency_key}>{x.incident_type} · {x.severity}</p>)}</div></main>}