'use client';import {useEffect,useState} from 'react';import {api} from '../../../lib/api';export default function Page(){const[r,setR]=useState<any[]>([]);useEffect(()=>{api('/api/v1/reports').then(setR)},[]);return <main className="safety"><h1 className="title">Assigned Incidents</h1>{r.map(x=><div className="card" key={x.id}><b>{x.incident_type}</b><p>{x.description||'No description'}</p><p>{x.severity} · {x.status}</p><button className="btn">Update status</button></div>)}</main>}
