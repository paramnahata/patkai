'use client';import {useEffect,useState} from 'react';import GovShell from '../../../components/GovShell';import {api} from '../../../lib/api';import {LineChart,Line,XAxis,YAxis,Tooltip,ResponsiveContainer,BarChart,Bar} from 'recharts';export default function Page(){const[a,setA]=useState<any>();useEffect(()=>{api('/api/v1/analytics').then(setA)},[]);return <GovShell title="Analytics"><h1 className="title">Risk & Response Analytics</h1><div className="grid dashboard-grid"><div className="card"><h3>Risk trend</h3><div style={{height:320}}>{a&&<ResponsiveContainer><LineChart data={a.risk_trend}><XAxis dataKey="time"/><YAxis/><Tooltip/><Line type="monotone" dataKey="risk"/></LineChart></ResponsiveContainer>}</div></div><div className="card"><h3>High-risk zones by district</h3><div style={{height:320}}>{a&&<ResponsiveContainer><BarChart data={a.districts}><XAxis dataKey="district"/><YAxis/><Tooltip/><Bar dataKey="incidents"/></BarChart></ResponsiveContainer>}</div></div></div></GovShell>}
