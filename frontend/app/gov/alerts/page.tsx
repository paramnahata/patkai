'use client';import {useEffect,useState} from 'react';import GovShell from '../../../components/GovShell';import {api} from '../../../lib/api';export default function Page(){const[a,setA]=useState<any[]>([]);useEffect(()=>{api('/api/v1/alerts').then(setA)},[]);return <GovShell title="Alerts"><h1 className="title">Alert Management</h1><div className="grid">{a.map(x=><div className="card" key={x.id}><span className={`badge badge-${x.level.toLowerCase()==='critical'?'critical':'high'}`}>{x.level}</span><h3>{x.title}</h3><b>{x.location}</b><p>{x.reason}</p><p><b>Recommended:</b> {x.action}</p><div className="muted">Source: {x.source} · {new Date(x.created_at).toLocaleString()}</div></div>)}</div></GovShell>}
