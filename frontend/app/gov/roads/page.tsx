'use client';import {useEffect,useState} from 'react';import GovShell from '../../../components/GovShell';import {api} from '../../../lib/api';export default function Page(){const[r,setR]=useState<any[]>([]);const load=()=>api('/api/v1/roads').then(setR);useEffect(()=>{load()},[]);return <GovShell title="Road Connectivity"><h1 className="title">Road Connectivity</h1><p className="muted">Green=open · yellow=at risk · orange=restricted · red=blocked.</p><div className="card"><table className="table"><thead><tr><th>Road</th><th>Route</th><th>District</th><th>Status</th><th>Risk</th><th>Last verified</th><th>Impact</th></tr></thead><tbody>{r.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.route_number}</td><td>{x.district}</td><td><span className="badge">{x.status}</span></td><td>{x.risk}</td><td>{new Date(x.last_verified).toLocaleString()}</td><td>{x.impact}</td></tr>)}</tbody></table></div></GovShell>}
