'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import {login} from '../../../lib/api';
export default function Login(){const [email,setEmail]=useState('admin@patkai.demo'),[password,setPassword]=useState('Patkai@2026'),[err,setErr]=useState('');const r=useRouter();return <main className="login"><div className="card login-card"><div className="brand">PATKAI</div><h1 className="title">Government Control Room</h1><p className="muted">AI-powered landslide early warning & decision support · NER</p><label>Email</label><input className="input" value={email} onChange={e=>setEmail(e.target.value)}/><label>Password</label><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)}/>{err&&<div className="notice">{err}</div>}<button className="btn btn-primary" style={{width:'100%'}} onClick={async()=>{try{const x=await login(email,password);localStorage.setItem('patkai_token',x.access_token);localStorage.setItem('patkai_user',JSON.stringify(x.user));r.push('/gov/dashboard')}catch(e:any){setErr(e.message)}}}>Sign in</button><p className="footer-note">Demo accounts use password <b>Patkai@2026</b>. Data shown is synthetic/demo data.</p></div></main>}
