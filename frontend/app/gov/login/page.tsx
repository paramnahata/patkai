'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import {login,saveSession} from '../../../lib/api';
const DEMOS=[['admin@patkai.demo','Government Admin'],['district@patkai.demo','District Officer'],['field@patkai.demo','Field Officer']];
export default function Login(){const [email,setEmail]=useState('admin@patkai.demo'),[password,setPassword]=useState('Patkai@2026'),[err,setErr]=useState('');const r=useRouter();return <main className="login"><div className="card login-card"><div className="brand">PATKAI</div><h1 className="title">Government Control Room</h1><p className="muted">AI-powered landslide early warning & decision support · NER</p><label>Demo role</label><select className="input" value={email} onChange={e=>setEmail(e.target.value)}>{DEMOS.map(x=><option key={x[0]} value={x[0]}>{x[1]} · {x[0]}</option>)}</select><label>Email</label><input className="input" value={email} onChange={e=>setEmail(e.target.value)}/><label>Password</label><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)}/>{err&&<div className="notice">{err}</div>}<button className="btn btn-primary" style={{width:'100%'}} onClick={async()=>{try{const x=await login(email,password);saveSession('gov',x);r.push('/gov/dashboard')}catch(e:any){setErr(e.message)}}}>Sign in</button><p className="footer-note">Demo password: <b>Patkai@2026</b>. Each role gets its own session and permissions.</p><a className="switch-link" href="/app">Citizen safety app →</a></div></main>}
