'use client';import {useEffect,useState} from 'react';import MobileShell from '../../../components/MobileShell';
export default function Page(){const[l,setL]=useState('English');useEffect(()=>setL(localStorage.getItem('patkai_language')||'English'),[]);const change=(v:string)=>{setL(v);localStorage.setItem('patkai_language',v)};return <MobileShell title="Settings"><div className="card"><b>Language</b><select className="input" value={l} onChange={e=>change(e.target.value)}><option>English</option><option>हिन्दी</option><option>অসমীয়া</option><option>বাংলা</option></select><p className="muted">Your language choice is stored on this phone. The prototype is structured for additional NER languages.</p></div><div className="card"><b>Offline safety</b><p>Latest safety information and report drafts can remain available when connectivity is lost.</p><span className="badge">OFFLINE READY</span></div><div className="card"><b>Data transparency</b><p className="muted">Environmental feeds shown in the prototype are labelled as demo/synthetic unless connected to an approved external provider.</p></div></MobileShell>}
