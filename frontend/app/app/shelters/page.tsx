'use client';import {useEffect,useState} from 'react';import MobileShell from '../../../components/MobileShell';import {api} from '../../../lib/api';export default function Page(){const[p,setP]=useState<any[]>([]);useEffect(()=>{api('/api/v1/places?kind=SHELTER&lat=25.27&lon=91.73').then(setP)},[]);return <MobileShell title="Safe Shelters">{p.map(x=><div className="card" key={x.id}><h3>{x.name}</h3><p>{x.distance_km} km · Capacity {x.capacity} · {x.status}</p><p>Contact: {x.contact}</p><button className="btn btn-primary" onClick={()=>alert(`Directions to ${x.name}`)}>Navigate to safe shelter</button></div>)}</MobileShell>}
