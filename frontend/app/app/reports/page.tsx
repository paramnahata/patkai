'use client';import {useEffect,useState} from 'react';import MobileShell from '../../../components/MobileShell';import {api} from '../../../lib/api';export default function Page(){const[r,setR]=useState<any[]>([]);useEffect(()=>{api('/api/v1/reports').then(setR)},[]);return <MobileShell title="My Reports">{r.map(x=><div className="card" key={x.id}><b>{x.incident_type}</b><p>{x.status} · {x.verification_status}</p><div className="muted">Trust {x.trust_score}/100</div></div>)}</MobileShell>}
