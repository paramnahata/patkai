'use client';import {useEffect,useState} from 'react';import MobileShell from '../../../components/MobileShell';import {api} from '../../../lib/api';export default function Page(){const[a,setA]=useState<any[]>([]);useEffect(()=>{api('/api/v1/alerts').then(setA)},[]);return <MobileShell title="Safety Alerts">{a.map(x=><div className="card" key={x.id}><span className="badge badge-critical">{x.level}</span><h3>{x.title}</h3><p>{x.location}</p><p>{x.reason}</p><b>Recommended:</b> {x.action}</div>)}</MobileShell>}
