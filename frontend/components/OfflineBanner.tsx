'use client';import {useEffect,useState} from 'react';export default function OfflineBanner(){const[o,setO]=useState(false);useEffect(()=>{const on=()=>setO(!navigator.onLine);on();addEventListener('online',on);addEventListener('offline',on);return()=>{removeEventListener('online',on);removeEventListener('offline',on)}},[]);return o?<div className="notice" style={{position:'sticky',top:0,zIndex:20}}><b>OFFLINE MODE</b> · Cached safety information and queued reports remain available.</div>:null}
