# Backend API used for authenticated workflows. The map itself does not require this to render.
NEXT_PUBLIC_API_URL=https://patkai.onrender.com

# Optional: MapTiler public key. Leave blank for the default keyless OpenStreetMap basemap.
NEXT_PUBLIC_MAPTILER_KEY=
